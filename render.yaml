services:
  - type: web
    name: task-app
    env: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /health
    dockerBuildArgs:
      VITE_API_BASE_URL: ""
      VITE_VAPID_PUBLIC_KEY: $VITE_VAPID_PUBLIC_KEY
